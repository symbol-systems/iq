remove hard-coded iq-server-dump.tar.gz - dump must always be specified/fully qualified.

The VFS should be in `iq-kernel` or `iq-platform` or ?elsewhere? but definitely not in `iq-secrets/iq-secrets-core/src/main/java/systems/symbol/vfs`
