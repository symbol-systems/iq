#!/usr/bin/env bash
# Run IQ MCP Server via unified CLI framework
# This is an alias for: ./bin/iq-cli-server start mcp
set -euo pipefail
cd "$(cd "$(dirname "$0")" && pwd)"
exec ./iq-cli-server mcp "$@"
