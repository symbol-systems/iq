#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
./mvnw -f iq-mcp/pom.xml compile exec:java -Dexec.mainClass=systems.symbol.MCPServerMain -Dexec.classpathScope=compile -DskipTests
