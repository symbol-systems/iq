#!/usr/bin/env bash
# Run IQ APIs in dev mode with live reload
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"
exec ./mvnw compile quarkus:dev -pl iq-apis -am -Dorg.eclipse.rdf4j.repository.debug="false" -DskipTests "$@"


