package systems.symbol.trust;

import org.eclipse.rdf4j.model.IRI;
import org.eclipse.rdf4j.model.Model;

public class X extends TrustedIntent {
public X(IRI self, Model model) {
super(self, model);
}
}
