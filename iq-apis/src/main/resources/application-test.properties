# Test profile - disable OpenTelemetry since we don't need tracing for unit tests
# and the Jaeger exporter is not required as a test dependency
quarkus.otel.enabled=false
quarkus.micrometer.enabled=true
